# ===== 一起旅行 SKILL · Python 依赖 =====
# 安装: pip install -r requirements.txt

# 核心: 图像处理 + EXIF (阶段 0.5+)
Pillow>=10.0.0
piexif>=1.1.3

# 环境变量加载 (.env 文件)
python-dotenv>=1.0.0

# HTTP 请求 (阶段 2 高德 POI 调用, 阶段 1.5 VLM 调用)
requests>=2.31.0

# PDF 生成 (阶段 6 HTML+PDF 双格式)
# 选 1:
#   reportlab (纯 Python, 适合简单报告)
reportlab>=4.0.0
#   weasyprint (HTML→PDF, 排版更丰富, 但依赖 cairo/pango 系统库)
# weasyprint>=60.0

# 可选: OCR 离线方案 (阶段 1.5 替代 VLM, 详见 README "OCR 让用户自配" 段)
# paddleocr>=2.7.0
# rapidocr_onnxruntime>=1.3.0

# 开发/测试
pytest>=7.4.0
pytest-cov>=4.1.0
